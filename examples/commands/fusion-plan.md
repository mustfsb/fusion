---
description: Run Fusion Council planning
agent: plan
---
Use the `fusion_council` tool with mode `plan`.

By default, use modelSource `auto`, panel models `opencode-go/kimi-k2.7-code`, `opencode-go/qwen3.7-max`, `opencode-go/minimax-m3`, and judge model `openai/gpt-5.5`.

Task: $ARGUMENTS

Include relevant project context automatically. Return the tool result and do not modify files.
