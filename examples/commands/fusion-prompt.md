---
description: Generate a build prompt with Fusion Council
agent: plan
---
Use the `fusion_council` tool with mode `build_prompt`.

By default, use modelSource `auto`, panel models `opencode-go/kimi-k2.7-code`, `opencode-go/qwen3.7-max`, `opencode-go/minimax-m3`, and judge model `openai/gpt-5.5`.

Prompt request: $ARGUMENTS

Return the generated prompt from the tool result and do not modify files.
