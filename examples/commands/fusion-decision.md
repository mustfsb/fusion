---
description: Run Fusion Council decision analysis
agent: plan
---
Use the `fusion_council` tool with mode `decision`.

By default, use modelSource `auto`, panel models `opencode-go/kimi-k2.7-code`, `opencode-go/qwen3.7-max`, `opencode-go/minimax-m3`, and judge model `openai/gpt-5.5`.

Decision question: $ARGUMENTS

Return the tool result and do not modify files.
